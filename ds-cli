#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.9"
# dependencies = ["PyYAML>=6,<7"]
# ///
"""ds-cli — Claude Code CLI proxy with configurable backend support.

Thin entry point — all logic lives in cli/ subpackage.
"""

import sys
import os

# Ensure cli/ package is importable
_ds_cli_dir = os.path.dirname(os.path.abspath(__file__))
if _ds_cli_dir not in sys.path:
    sys.path.insert(0, _ds_cli_dir)

from cli.main import main

if __name__ == "__main__":
    main()
